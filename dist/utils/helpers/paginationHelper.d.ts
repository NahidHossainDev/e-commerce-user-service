import { IPaginateCalculateResult, IPaginationQuery } from 'src/common/interface';
export declare const paginationHelpers: {
    calculatePagination: (options: IPaginationQuery) => IPaginateCalculateResult;
};
